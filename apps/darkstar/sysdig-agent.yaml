---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: flux-sysdig-helm-release
  namespace: sysdig-agent
spec:
  interval: 10m
  chart:
    spec:
      version: "1.65.6"
  values:
    global:
      clusterConfig:
        name: darkstar
